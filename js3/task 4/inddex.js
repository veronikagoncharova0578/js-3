const maxage = 55;
if (maxage > 0 && maxage < 16) {
    console.log("Дети")
} 
if (maxage > 17 && maxage < 60) {
    console.log("Взрослые")
}
if (maxage > 61 && maxage < 100) {
    console.log(Пенсионеры)
}