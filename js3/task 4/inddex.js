const maxage = 55;
if (maxage > 0 && maxage < 17) {
    console.log("Дети")
} 
if (maxage > 17 && maxage < 61) {
    console.log("Взрослые")
}
if (maxage > 61 && maxage < 100) {
    console.log(Пенсионеры)
}